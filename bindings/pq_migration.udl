namespace pq_migration {
  enum MobileMigrationMode { "Legacy", "Hybrid", "QuantumSafe" };

  record MercyLedger {};

  MercyLedger create_mercy_ledger(MobileMigrationMode mode);
  void commit(MercyLedger ledger, bytes data);
  void migrate(MercyLedger ledger, MobileMigrationMode new_mode);
};
